import 'package:flutter/material.dart';

class AdminRoutesListPage extends StatelessWidget {
  const AdminRoutesListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rutas')),
      body: const Center(
        child: Text('Listado de rutas (pendiente implementar)'),
      ),
    );
  }
}
