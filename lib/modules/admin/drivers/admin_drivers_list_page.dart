import 'package:flutter/material.dart';

class AdminDriversListPage extends StatelessWidget {
  const AdminDriversListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Conductores')),
      body: const Center(
        child: Text('Listado de conductores (pendiente implementar)'),
      ),
    );
  }
}
